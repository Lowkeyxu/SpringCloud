server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Lowkeyxu/SpringcloudConfig/
          searchPaths: respo
    label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8009/eureka/